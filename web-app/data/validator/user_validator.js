function validateUser(user) {

    // var username = document.getElementById('username');
    // var password = document.getElementById('inputPassword');
    // var firstname = document.getElementById('firstname');
    // var lastname = document.getElementById('lastname');
    // var email = document.getElementById('Email');
    // var address = document.getElementById('inputAddress');
    // var phonenumber = document.getElementById('phonenumber');
    // var state = document.getElementById('inputState');
    // var city = document.getElementById('inputCity');
    // var zip = document.getElementById('inputZip');
    
    // if(username.value == "" || password.value == "" || firstname.value== "" || lastname.value=="" || email.value == "" || address.value == "" || phonenumber.value == "" || state.value == "" || city.value == "" || zip.value == "")
    // {
    //     throw "Please fill all the values in the form."
    // } 
    // else{
        
    // }
        //user generic.validator.js to define common validation task
    //validate first name
    //validate last name
    //validate phone number
    //validate email address
    //validate address
}
module.exports = {
    validateUser
}